{% extends 'base.html.twig' %}

{% block container %}

    <h1>Chat Rooms</h1>
    <ul>
    {% for chatRoom in chatRooms %}
        <li><a href="{{ path('chat_room', {id: chatRoom.id}) }}">{{ chatRoom.name }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}